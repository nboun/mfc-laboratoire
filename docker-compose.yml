version: "3.8"
services:
  mfc:
    build: .
    container_name: mfc-laboratoire
    ports:
      - "3000:3000"
    volumes:
      - ./mfc-data:/data
    environment:
      - MFC_DATA_DIR=/data
      - PORT=3000
    restart: always
